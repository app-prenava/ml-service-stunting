version: '3.8'

services:
  ml-api:
    build: .
    container_name: prenava-ml-api
    restart: always
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
    # Uncomment the networks section if you want to connect it to your Laravel container's network
    # networks:
    #   - prenava_network

# networks:
#   prenava_network:
#     external: true
